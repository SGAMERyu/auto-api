export enum HTTP_METHODS {
  GET = "get",
  PUT = "put",
  POST = "post",
  DELETE = "delete",
  OPTIONS = "options",
  HEAD = "head",
  PATCH = "patch",
}

export enum PARAMS_TYPE {
  QUERY = "query",
  HEADER = "header",
  PATH = "path",
  FORM_DATA = "formData",
  BODY = "body",
}

export enum HTTP_STATUS_CODE {
  "SUCCESS" = "200",
}
